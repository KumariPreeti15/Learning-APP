import { OmniElement } from '../../omni-element.js';
/**
 * To include within your project `import { OmniAccordion } from 'omni-ui/omni-accordion.js';`
 *
 * @tagname omni-accordion
 * @slot - slot for the content of the accordion
 * @slot summary - slot for the summary of the accordion
 * @event {boolean} toggle - fires when the accordion is opened or closed
 */
export declare class OmniAccordion extends OmniElement {
    #private;
    static get styles(): import("lit").CSSResult[];
    private _details;
    private _body;
    private _initialHeight;
    private _finalHeight;
    animateAccordion: boolean;
    open: boolean;
    name: string;
    variant: 'default' | 'highlighted';
    header: string;
    subheader: string;
    subplacement: 'inline' | 'bottom';
    position: 'left' | 'right';
    disabled: boolean;
    firstUpdated(): Promise<void>;
    updated(changed: any): void;
    close(): Promise<void>;
    render(): import("lit-html").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'omni-accordion': OmniAccordion;
    }
}
//# sourceMappingURL=omni-accordion.d.ts.map