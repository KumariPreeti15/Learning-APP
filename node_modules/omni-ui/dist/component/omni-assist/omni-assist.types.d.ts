import type { TemplateResult } from 'lit';
import type { SelectableItem } from '../omni-dropdown/omni-dropdown.types.js';
export interface ChatHistoryItem {
    role: 'user' | 'assistant';
    content: string | TemplateResult;
    /** any number, Date, string that will construct with a js Date object */
    timestamp?: number | Date | string;
}
export interface ChatSuggestion {
    prompt: string;
    category?: string;
}
export type Vote = 'upvote' | 'downvote';
export interface Feedback {
    fsl_type: Vote;
    prompt: string;
    correct_completion?: string;
    incorrect_completion?: string;
    user_comment?: string;
    reason?: Record<string, unknown>;
}
export interface GenAiModel extends SelectableItem {
    modelName: 'gpt-3.5' | 'gpt-4' | 'claude-2.1' | 'claude-3' | 'gemini-1-pro' | 'gemini-1.5-pro' | 'gemini-1.5-flash' | 'llama3';
    value: 'GPT-3.5' | 'GPT-4' | 'Claude 2.1' | 'Claude 3' | 'Gemini 1 Pro' | 'Gemini 1.5 Pro' | 'Gemini 1.5 Flash' | 'Llama 3';
}
//# sourceMappingURL=omni-assist.types.d.ts.map